<div id="hr-absence-edit-modal" class="hr-tc-popup" role="dialog" aria-modal="true" aria-hidden="true"
     data-csrf="<%= form_authenticity_token %>">
  <div class="hr-tc-popup-inner hr-tc-absence-modal-inner">
    <h3 class="hr-tc-absence-modal-title"><%= l(:label_hr_absence_edit_title, kind: '') %></h3>
    <form id="hr-absence-edit-form" autocomplete="off">
      <div class="hr-tc-absence-modal-row">
        <label><%= l(:label_hr_absence_starts_on) %></label>
        <input type="date" name="starts_on" id="hr_absence_edit_starts_on" required>
      </div>
      <div class="hr-tc-absence-modal-row">
        <label><%= l(:label_hr_absence_ends_on) %></label>
        <input type="date" name="ends_on" id="hr_absence_edit_ends_on" required>
      </div>
      <div class="hr-tc-absence-modal-row">
        <label><%= l(:label_hr_absence_reason) %></label>
        <textarea name="reason" id="hr_absence_edit_reason" rows="3"></textarea>
      </div>
      <% if User.current.admin? %>
        <div class="hr-tc-absence-modal-row hr-tc-edit-status-row">
          <label><%= l(:label_hr_absence_status) %></label>
          <select name="status" id="hr_absence_edit_status">
            <option value="requested"><%= l(:label_hr_absence_status_requested) %></option>
            <option value="approved"><%= l(:label_hr_absence_status_approved) %></option>
            <option value="rejected"><%= l(:label_hr_absence_status_rejected) %></option>
          </select>
        </div>
      <% end %>
      <div class="hr-tc-absence-modal-actions">
        <button type="submit" class="button-positive"><%= l(:button_hr_absence_save) %></button>
        <button type="button" class="hr-tc-absence-modal-cancel"><%= l(:button_cancel) %></button>
      </div>
    </form>
  </div>
</div>
